# Prism Metabolic Console 项目介绍

本文档面向第一次接触本项目的读者，目标是让产品、研发、合作方或评审人员快速理解 Prism Metabolic Console 是什么、当前做到什么程度、已经具备哪些能力、竞争力在哪里，以及后续还需要补齐什么。

## 1. 项目一句话定位

Prism Metabolic Console 是一个面向代谢健康管理的智能饮食记录与饮食决策系统。它以“食鉴 AI”为用户入口，结合饮食日志、健康档案、慢病/过敏本地规则库和豆包多模态大模型，帮助用户更低成本地记录吃了什么，并在高血压、高尿酸/痛风、2 型糖尿病、高脂血症等代谢场景下获得更保守、更可解释的饮食建议。

它不是单纯的卡路里记录工具，也不是只靠大模型自由回答的聊天机器人。它的核心思想是：先用本地结构化规则处理明确风险，再让云端 AI 做解释、补充和交互。

## 2. 项目要解决的问题

普通饮食 App 通常只关注热量，无法处理多病共存、过敏、嘌呤、钠、血糖和饮食习惯之间的冲突。普通 AI 聊天又容易在医疗健康场景中给出不稳定、不可追溯、过度乐观的建议。

本项目试图解决以下问题：

- 用户记录饮食太麻烦，导致健康数据难以连续沉淀。
- 慢病用户真正关心的不只是热量，而是钠、嘌呤、糖、脂肪、过敏原和烹饪方式。
- 多病共存时，经常出现“对减脂有利但对痛风不利”“低糖但高钠”“高蛋白但高嘌呤”等冲突。
- AI 建议需要有边界，尤其在过敏、限制、避免类场景下不能被大模型放宽。
- 健康建议需要能解释来源，至少要知道是本地规则、本地知识还是云端补充。
- 产品需要具备从本地开发到云端部署的完整链路，便于演示、试用和继续迭代。

## 3. 当前阶段判断

截至当前代码状态，项目已经达到“功能型 MVP 到 Alpha+”阶段。

这意味着：

- 已经不是静态原型，前端页面、后端 API、数据库模型、AI 服务和本地规则引擎已经连成闭环。
- 可以完成注册登录、健康档案维护、饮食记录、AI 对话、拍照识别、语音解析、候选确认、离线缓存和同步等核心流程。
- 已有本地开发脚本、Docker/云端部署说明、Alembic 数据库迁移和后端测试用例。
- 可以作为演示版、试用版、投融资/路演原型、继续研发的技术底座。

但它还不是完全生产级商业化产品：

- 验证码目前是开发版逻辑，后端生成后直接返回给前端，尚未接入真实短信网关。
- 本地知识库仍是 core_v1 基础覆盖，当前覆盖 4 类代谢相关病种、26 个食物、104 条病种-食物规则、10 个知识来源。
- 体检报告上传与分析、头像上传、用户协议、隐私政策、正式菜谱推荐等功能在界面上有占位或明确标注为未开放。
- 医疗健康合规、生产监控、限流、防刷、数据隐私治理、完整 QA 和真实用户验证仍需继续完善。

阶段结论：项目已经具备可运行、可演示、可继续工程化扩展的产品骨架和核心能力。下一步重点应从“能跑通”转向“可信、可规模化、可运营”。

## 4. 目标用户与使用场景

目标用户主要包括：

- 有高血压、高尿酸/痛风、2 型糖尿病、高脂血症等代谢风险的人群。
- 需要记录饮食、控制体重、关注钠和嘌呤摄入的人群。
- 对某些食物过敏，吃饭前需要风险提醒的人群。
- 健康管理师、营养师、慢病管理机构，用于辅助记录和初筛。
- 需要展示 AI 健康管理能力的产品团队或商业合作方。

典型使用场景：

- 用户拍一张饭菜照片，系统识别食物、估算营养、提示风险，再由用户确认写入日志。
- 用户说“中午吃了一碗米饭、两个鸡蛋、一份排骨”，系统解析语音文本并自动生成可确认的饮食记录。
- 用户询问“痛风能不能吃虾”，系统先查本地规则，命中限制或避免时不给云端放宽结论，只让 AI 解释原因和替代方案。
- 用户每天打开首页查看热量、钠、嘌呤余额，判断今天还能吃多少。
- 用户在健康档案里录入高血压或痛风后，每日钠和嘌呤目标会自动收紧。

## 5. 当前已经能做到什么

### 5.1 账户与登录

已实现：

- 手机号注册。
- 密码登录。
- 验证码登录。
- 通过验证码重置密码。
- JWT access token 与 refresh token。
- token 过期后的前端自动刷新。
- 登录失败、token 无效时自动退出。
- 游客模式，可进入首页查看演示数据。

当前边界：

- 验证码是开发模式，真实短信发送还没有接入。
- 生产环境需要接入短信供应商、频控、IP 限流、设备指纹、防刷策略和日志脱敏。

### 5.2 首页代谢概览

首页提供“今日代谢余额”视图，聚焦三个关键指标：

- 热量余额。
- 钠摄入余额。
- 嘌呤摄入余额。

系统会根据当天已记录饮食进行累计，并和每日目标对比。对于高血压用户，钠目标会更保守；对于痛风或高尿酸用户，嘌呤目标会更保守。

首页还包含：

- AI 智能洞察消息入口。
- 未读消息提示。
- 推荐安全提示占位，明确说明未经规则校验的个性化菜谱推荐暂未开放。

### 5.3 饮食日志

饮食日志是当前产品的核心数据入口之一。

已实现：

- 按日期查看饮食记录。
- 前一天、后一天、日期选择和回到今天。
- 新增餐食记录。
- 编辑餐食名称、份量、餐次、分类、热量、钠、嘌呤、蛋白质、碳水、脂肪、膳食纤维和备注。
- 删除餐食记录。
- 早餐、午餐、晚餐、加餐分类。
- 主食、肉蛋、蔬果、饮品、零食分类。
- 每日热量、钠、嘌呤、蛋白质、碳水、脂肪汇总。
- BMI、BMR 范围和推荐热量目标展示。
- 随机健康建议卡片。

手动新增餐食时，前端会使用确定性规则估算热量、钠和嘌呤。它不是随机 AI 结果，而是根据食物分类、份量数字和备注关键词进行固定修正。例如“咸、盐、酱、卤”会提高钠估算，“炸、油、煎、肥”会提高热量估算，“汤、内脏、海鲜”会提高嘌呤估算。

当前边界：

- 手动估算是粗粒度规则，适合快速记录和演示，不等于精确营养数据库。
- 离线待同步记录暂不支持编辑或删除，前端会提示联网同步后再操作。

### 5.4 语音饮食录入

聊天界面内已接入浏览器语音识别能力。用户可以用中文描述刚吃的内容，前端拿到 transcript 后交给后端 intake 服务解析。

已实现：

- 中文语音转文字入口。
- 从文本中切分多个食物片段。
- 识别常见份量，如“100g”“一碗”“两个”“半杯”。
- 推断餐次，如早餐、午餐、晚餐、加餐。
- 匹配本地食物库和疾病规则。
- 生成候选饮食项。
- 自动确认写入日志，失败项保留给用户手动确认。

当前边界：

- 语音识别依赖浏览器能力，不同浏览器兼容性不同。
- 复杂自然语言、模糊分量和地方菜名仍需要继续增强。

### 5.5 拍照识别与图片录入

项目支持两条图片识别路径：

- 相机页直接拍照或从相册选择图片。
- 聊天页上传图片并附加文字提示。

已实现：

- 调用摄像头。
- 后置摄像头优先。
- 支持闪光灯能力检测与开关。
- 从相册上传图片。
- 图片压缩优化后上传。
- 后端通过豆包主多模态模型识别食物。
- 返回食物名称、置信度、估算份量、主要食材、烹饪方式、热量、钠、嘌呤、蛋白质、碳水、脂肪、膳食纤维、糖、分类、过敏标签和风险标签。
- 图片识别后再次进入本地规则评估。
- 生成候选项，让用户确认后再正式写入饮食日志。

这条链路的设计比较重要：AI 识别不是直接入库，而是先生成候选，再由用户确认。这样可以降低误识别带来的数据污染。

### 5.6 候选确认工作流

语音、拍照和 AI 快捷记录都会进入统一的候选确认组件。

用户可以在写入日志前检查和修改：

- 食物名称。
- 餐次。
- 食物分类。
- 份量和单位。
- 原始份量描述。
- 主要食材。
- 烹饪方式。
- 热量、钠、嘌呤。
- 备注。

候选卡会展示：

- 推荐级别，如 RECOMMEND、MODERATE、CONDITIONAL、LIMIT、AVOID、INSUFFICIENT。
- 置信度。
- 是否命中本地规则。
- 命中的病种。
- 风险警告。
- 规则来源。
- 估算字段标记。

如果用户修改了候选项，系统会要求先重新评估，再允许写入日志。这能避免用户修改食物名称后仍沿用旧规则结论。

### 5.7 AI 健康顾问

AI 顾问名为“食鉴 AI”，定位为 PRISM 智能健康引擎。

已实现：

- 创建聊天会话。
- 恢复历史会话。
- 保存用户消息和 AI 回复。
- Markdown 渲染，并使用 DOMPurify 做安全净化。
- SSE 流式回复。
- 前端展示生成状态，如检查本地规则、连接模型、生成回复。
- 记录 request_id 和流式性能日志。
- AI 回复可附带知识来源、fallback 状态和审计信息。
- 支持图片识别结果在聊天中展示，并转入候选确认。

AI 不是直接裸连大模型。后端会先做本地知识总结：

- 如果本地规则已经足够回答，直接返回本地规则答案。
- 如果命中过敏、AVOID 或 LIMIT 约束，则阻断云端放宽结论。
- 如果本地只命中部分知识，允许云端补充解释和替代建议。
- 如果本地完全未命中，允许云端兜底，但仍带上用户健康档案和安全提示词。

当前边界：

- 聊天界面有“分析师模式/教练模式”的 UI 提示，但核心回答策略主要由后端系统提示词和本地规则决定。
- AI 能力依赖火山方舟/豆包 API Key 和模型 endpoint。
- AI 建议仅用于健康饮食参考，不构成医疗诊断。

### 5.8 健康档案

健康档案用于记录用户慢性病和过敏信息，是本项目个性化和安全判断的基础。

已实现：

- 添加慢性病或过敏档案。
- 编辑健康档案状态、趋势、指标值、单位、描述和归因。
- 删除健康档案。
- 区分慢性病和过敏。
- 支持 ACTIVE、MONITORING、STABLE、ALERT 等状态。
- 支持 IMPROVED、WORSENING、STABLE 趋势。
- 个人主页展示病史背景。
- 过敏信息会进入规则引擎，作为硬性约束。

当前边界：

- 健康档案的医学结构化程度还比较基础。
- 体检报告原始档案入口已存在，但真实上传与分析接口未开放。

### 5.9 每日目标计算

后端统一计算每日目标，避免前后端各算一套。

已实现：

- BMI 计算。
- BMR 计算和 BMR 范围。
- 默认活动系数。
- TDEE 估算。
- 根据 BMI 分类给出维持、增重、轻度减重、减重策略。
- 男性、女性和未知性别的安全热量下限。
- 高血压场景下钠目标收紧为 1500mg。
- 高尿酸/痛风场景下嘌呤目标收紧为 300mg。
- 资料不完整时返回“请完善资料”的估算说明。

这让首页和日志页的“余额”不只是固定数字，而是跟用户身体资料和健康档案动态关联。

### 5.10 本地知识库与规则引擎

本地知识库是项目的重要差异化模块。

当前 core_v1 已覆盖：

| 类型 | 当前数量 | 说明 |
| --- | ---: | --- |
| 病种 | 4 | 高血压、高脂血症、高尿酸/痛风、2 型糖尿病 |
| 食物 | 26 | 主食、蔬果、肉类、水产、豆制品、奶类、饮品、零食等 |
| 病种-食物规则 | 104 | 4 个病种各 26 条规则 |
| 来源 | 10 | 指南、共识、FAQ、教育材料等 |

规则引擎能力：

- 健康档案标准化，把“高尿酸”“痛风”“hyperuricemia”等映射到稳定 disease_code。
- 食物名称和别名匹配。
- 过敏和显式忌口硬阻断。
- 病种-食物规则评估。
- 多病共存时选择更严格的推荐级别。
- 输出份量建议、频率建议、摘要说明、冲突说明、注意事项。
- 输出来源引用，支持 source tier、source type、年份、发布机构等字段。
- 输出 fallback 状态，说明是否可以调用云端。

核心 fallback 状态：

| 状态 | 含义 |
| --- | --- |
| LOCAL_COMPLETE | 本地知识已足够回答，不需要云端 |
| LOCAL_PARTIAL_ALLOW_CLOUD | 本地命中部分知识，允许云端补充 |
| LOCAL_BLOCKED_NO_CLOUD | 本地命中过敏、AVOID 或 LIMIT，云端不得放宽 |
| NO_LOCAL_MATCH_ALLOW_CLOUD | 本地未命中，允许云端兜底 |

这套设计使 AI 在高风险饮食建议上更可控。

### 5.11 知识审计

后端有 knowledge_audit_logs 表，用于记录关键知识判断。

审计字段包括：

- 用户 ID。
- 路由名称。
- 聊天会话和消息 ID。
- 查询摘要。
- 回答来源，如 LOCAL_RULE、LOCAL_KNOWLEDGE、CLOUD_SUPPLEMENT、MIXED。
- fallback 状态。
- 命中的病种和食物。
- 未能映射的健康档案。
- 本地决策级别。
- 是否调用云端。
- 云端调用原因。
- 云端阻断原因。

这为后续合规审查、质量追踪和规则迭代提供基础。

### 5.12 离线优先与同步

前端使用 Dexie.js 封装 IndexedDB，实现饮食日志的离线缓存。

已实现：

- 本地保存饮食记录。
- 按用户隔离本地缓存。
- 使用 client_id 作为离线同步的幂等标识。
- syncStatus 支持 PENDING、SYNCED、CONFLICT。
- 联网时自动同步。
- 默认每 5 分钟触发同步。
- 浏览器 online 事件触发同步。
- 服务端按 user_id 和 client_id 去重。
- 本地合并服务端记录。
- 已同步且超过 30 天的本地缓存可清理。
- 设置页展示缓存统计和清理入口。

当前边界：

- 冲突处理策略目前以服务器数据为准，复杂双向编辑冲突还没有完整交互式解决方案。

### 5.13 消息通知

已实现：

- 消息列表。
- 未读数量。
- 标记单条已读。
- 全部标记已读。
- 删除消息。
- 支持 WARNING、ADVICE、BRIEF 等消息类型。
- 首页展示最新消息和未读提醒。

当前边界：

- 消息生成机制目前不是完整运营系统，更多是为健康预警和建议推送预留接口。

## 6. 技术架构

整体架构如下：

```text
用户浏览器
  |
  | React + TypeScript + Vite
  | IndexedDB 离线缓存
  v
FastAPI 后端
  |
  | JWT 认证、REST API、SSE 流式响应
  | SQLAlchemy 2.0 异步 ORM、Alembic 迁移
  v
PostgreSQL
  |
  | 用户、餐食、健康档案、聊天、消息、知识库、审计日志
  v
本地规则引擎 + 豆包主多模态模型
```

前端技术栈：

- React 19。
- TypeScript。
- Vite。
- Dexie.js / IndexedDB。
- marked + DOMPurify。
- 浏览器 MediaDevices 相机能力。
- 浏览器 SpeechRecognition 语音识别能力。

后端技术栈：

- FastAPI。
- Python 3.9+。
- PostgreSQL 16。
- SQLAlchemy 2.0 async。
- Alembic。
- JWT。
- Pydantic v2。
- Volcengine ARK Runtime / 豆包主多模态模型。
- Pytest。
- Docker。

数据层：

- users：用户账号和身体参数。
- meals：饮食记录、来源、营养估算、识别元信息。
- health_conditions：慢性病和过敏档案。
- chat_sessions / chat_messages：AI 会话和消息。
- app_messages：系统通知。
- diseases / food_items / disease_food_rules / knowledge_sources / rule_source_maps / health_condition_mappings：本地知识库。
- knowledge_audit_logs：知识判断审计。

## 7. 主要 API 模块

| 模块 | 代表端点 | 功能 |
| --- | --- | --- |
| 认证 | /api/auth/* | 注册、登录、验证码、重置密码、刷新 token、用户资料、每日目标 |
| 饮食 | /api/meals/* | 新增、查询、编辑、删除、每日汇总、离线同步 |
| AI 对话 | /api/chat/* | 会话、消息、流式回复、图片识别、快捷记录 |
| 多模态录入 | /api/intake/* | 语音解析、语音自动入账、拍照识别解析、候选确认、重新评估 |
| 健康档案 | /api/conditions/* | 慢性病和过敏 CRUD |
| 知识库 | /api/knowledge/* | 病种、食物、规则、来源、食物评估、知识总结 |
| 消息 | /api/messages/* | 消息列表、未读数、已读、删除 |
| 健康检查 | /api/health | 后端运行状态 |

## 8. 用户完整流程示例

一个新用户可以这样使用：

1. 注册或登录账号。
2. 在设置里完善性别、年龄、身高、体重。
3. 在病史背景里添加高血压、高尿酸、糖尿病或过敏信息。
4. 回到首页查看今日热量、钠、嘌呤余额。
5. 吃饭后进入生命日志手动记录，或使用语音说出吃了什么。
6. 如果有图片，打开食物扫描或在聊天页上传图片。
7. 系统识别并生成候选项，用户确认食物、份量和风险提示。
8. 确认后写入生命日志。
9. 用户向食鉴 AI 咨询某个食物能不能吃。
10. 后端先查本地规则，如果命中限制或避免，则本地规则优先，云端不得放宽。
11. 用户持续记录后，可以在首页看到当天关键指标余额变化。

## 9. 项目竞争力

### 9.1 不只是热量管理，而是代谢风险管理

很多饮食工具只做热量和三大宏量素。本项目把钠、嘌呤、过敏、慢病状态和食物规则放在核心位置，更贴近高血压、高尿酸/痛风、糖尿病等真实健康管理场景。

### 9.2 本地规则优先，降低 AI 健康建议风险

项目不是把所有问题都交给大模型。对于过敏、AVOID、LIMIT 等明确风险，本地规则会直接阻断云端放宽结论。AI 只能解释原因、补充替代方案或说明适用条件。

这在医疗健康类 AI 产品里非常关键。

### 9.3 多模态低摩擦记录

饮食记录最难的是坚持。本项目同时提供：

- 手动记录。
- 语音记录。
- 拍照识别。
- 聊天页图片上传。
- AI 识别后候选确认。

这比单一表单录入更接近真实用户习惯。

### 9.4 可解释、可审计

知识规则带来源，AI 回复带 origin 和 fallback 状态，后端写审计日志。后续无论是做质量评估、合规审查还是知识库迭代，都有基础数据可以追踪。

### 9.5 前后端和部署链路完整

项目已经具备：

- Vite 前端。
- FastAPI 后端。
- PostgreSQL 数据库。
- Alembic 迁移。
- Dockerfile 和 docker-compose。
- 本地一键启动脚本。
- Vercel + Render + Neon 免费部署方案说明。

这使项目不是一次性演示页，而是可以持续迭代的工程系统。

### 9.6 离线优先能力

饮食记录支持本地缓存和联网后同步。对于移动 Web 使用场景，网络波动很常见，离线优先可以明显提高记录成功率。

## 10. 当前局限和风险

需要明确说明的是，本项目当前仍有以下局限：

- 不构成医疗诊断，不能替代医生或注册营养师。
- 本地知识库覆盖仍有限，当前是 core_v1 基础版本。
- AI 识别的食物和营养值属于估算，不是实验室检测数据。
- 图片识别依赖外部多模态模型，受 API Key、模型权限、网络和费用影响。
- 浏览器语音识别和相机能力存在设备兼容性差异。
- 真实短信、限流、防刷、隐私协议、用户协议、生产监控还未完全落地。
- 体检报告分析、个性化菜谱推荐、头像上传等仍处于未开放或待接入状态。
- 当前冲突同步策略偏简单，复杂多端编辑需要进一步设计。

这些边界不会否定项目价值，但需要在对外介绍、演示和试点时说清楚。

## 11. 适合如何对外介绍

可以这样介绍本项目：

Prism Metabolic Console 是一个面向慢病和代谢健康场景的 AI 饮食管理系统。它通过拍照、语音和聊天降低饮食记录成本，通过本地规则库处理高血压、痛风、糖尿病、高脂血症和过敏等风险，再由大模型提供解释和交互。相比普通卡路里 App，它更关注钠、嘌呤、过敏和多病共存冲突；相比普通健康 AI，它把明确风险放在本地规则里，并保留引用和审计记录，因此更可控、更可解释，也更适合继续走向健康管理产品化。

## 12. 后续路线建议

短期优先级：

1. 接入真实短信服务，补齐验证码生产能力。
2. 为认证接口增加频控、IP 限流和防刷策略。
3. 扩展 core_v1 知识库，增加更多常见中国饮食、地方菜和复合菜。
4. 为图片识别和语音录入增加更多端到端测试。
5. 完善错误提示和空状态，增强真实用户试用体验。
6. 建立知识库审核流程，区分草稿、审核通过和发布版本。

中期优先级：

1. 开放经过规则校验的个性化菜谱推荐。
2. 接入体检报告上传、OCR 或结构化解析。
3. 增加趋势分析，例如周/月钠摄入、嘌呤摄入、热量缺口。
4. 增加运营后台，用于管理知识来源、规则、消息和审计记录。
5. 增加更细粒度的同步冲突解决策略。
6. 加入生产级日志、监控、告警和成本观测。

长期方向：

1. 发展为慢病饮食管理平台。
2. 与营养师、健康管理师或医疗机构的服务流程结合。
3. 建立可版本化、可审核、可追踪的营养知识库。
4. 支持更多疾病组合、药食交互和个体化目标。
5. 从移动 Web 进一步扩展为 PWA 或原生 App。

## 13. 结论

Prism Metabolic Console 当前已经具备一个 AI 健康饮食产品的核心闭环：用户档案、饮食记录、多模态录入、AI 交互、本地规则、安全阻断、知识引用、审计日志和部署链路。

它最值得被认可的地方，不是“用了大模型”，而是把大模型放进了一个更稳健的健康决策框架里：能本地判断的先本地判断，必须保守的绝不放宽，需要解释和陪伴的再交给 AI。这个方向让项目既有产品体验，也有工程可扩展性和健康场景的可信度基础。
