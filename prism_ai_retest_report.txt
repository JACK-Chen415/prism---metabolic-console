Prism Metabolic Console AI Retest Report
Generated: 2026-05-22 23:20 Asia/Shanghai
Workspace: [REDACTED_LOCAL_WORKSPACE]

Sanitization
- ARK_API_KEY: [REDACTED]
- No full secrets are included in this report.

Requested Temporary AI Config
- ARK_API_KEY=[REDACTED]
- endpoint/model id: [REDACTED]

Config Path Actually Used
- backend/app/core/config.py defines Settings.model_config env_file=".env".
- scripts/start-local.ps1 sets current directory to the backend directory before running uvicorn.
- Therefore the live backend reads backend/.env when launched through the repo's established start-local.ps1 pattern.
- backend/app/core/config.py main_doubao_model returns DOUBAO_MODEL first, then falls back to deprecated DOUBAO_ENDPOINT_ID.
- backend/app/services/ai_service.py passes settings.main_doubao_model as the Ark chat completion model for both text and image/multimodal calls.
- Effective variable for the supplied endpoint/model id: DOUBAO_MODEL=[REDACTED].

Actions Performed
- Backed up backend/.env to backend/.env.backup.ai-retest-[REDACTED] before changing it.
- Temporarily set backend/.env ARK_API_KEY=[REDACTED] and DOUBAO_MODEL=[REDACTED].
- Attempted full backend restart with repo WSL/Windows hybrid script:
  powershell.exe -NoLogo -NoProfile -ExecutionPolicy Bypass -File <scripts/start-local.ps1> -SkipFrontend -SkipSeed
- The PowerShell interop call failed before the script ran with:
  <3>WSL (2 - ) ERROR: UtilBindVsockAnyPort:307: socket failed 1
- Attempted local health probe from this shell:
  curl http://localhost:8000/api/health
  Result: curl (7) Failed to connect to localhost port 8000.
- Checked whether a Linux fallback backend could be launched in this namespace. Python 3.12.3 is present, but required backend packages are not installed: fastapi, uvicorn, sqlalchemy, asyncpg, pydantic_settings, volcenginesdkarkruntime.
- The available backend/venv is a Windows virtualenv under backend/venv/Scripts and cannot be used without Windows interop.
- Restored backend/.env from backend/.env.backup.ai-retest-[REDACTED] because the supplied cloud config was not objectively validated.

E2E Results
- Health endpoint: not completed. Blocked by sandbox isolation from localhost:8000 and failed Windows interop.
- Throwaway user registration/reuse: not completed for the same reason.
- Chat session creation: not completed for the same reason.
- Text message cloud call: not completed; no objective evidence that the supplied temporary key called the cloud model.
- Image recognition endpoint: not completed; no objective evidence that image recognition is usable with the supplied temporary key.

Latency Findings
- Current-run latency findings: unavailable because the live E2E text chat request could not be executed.
- Existing code instrumentation logs chat_timing with doubao_total_ms, prompt_build_ms, prompt_chars, history_query_ms, knowledge_summary_ms, DB flush/audit timings, and response_chars in backend/.runtime/backend.dev.log when chat requests complete.
- Likely bottlenecks to validate once the environment can run:
  1. Cloud model generation time, especially because chat uses a very large system prompt and max_tokens defaults to DOUBAO_CHAT_MAX_TOKENS=800.
  2. Prompt size: SYSTEM_PROMPT plus user health context and recent history can be large; reducing prompt_chars should reduce model latency.
  3. Non-streaming /messages waits for full generation before responding. The /messages/stream endpoint should improve perceived latency by emitting first chunks.
  4. Knowledge summary and DB/audit writes are instrumented and should be compared against doubao_total_ms, but are likely secondary unless logs show otherwise.

Recommended Next Run
- Run from a shell with Windows interop and localhost networking enabled, then execute scripts/start-local.ps1 fully.
- Use a query unlikely to be answered locally so attachments.knowledge.called_cloud=true and chat_timing.cloud_called=true.
- Capture request wall time with curl and match X-Request-ID to backend/.runtime/backend.dev.log chat_timing.
- For image recognition, require success=true, foods length > 0, plausible food_name/nutrition fields, and ai_response that reflects the test image, not just HTTP 200.

Final State
- backend/.env restored from the backup because the supplied config could not be validated in this sandbox.
- The local backup filename is redacted from this tracked report.
